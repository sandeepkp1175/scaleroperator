# This rule is not used by the project scaleroperator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to api.sandeeppattnaik.online resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: scaleroperator
    app.kubernetes.io/managed-by: kustomize
  name: scaler-viewer-role
rules:
- apiGroups:
  - api.sandeeppattnaik.online
  resources:
  - scalers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - api.sandeeppattnaik.online
  resources:
  - scalers/status
  verbs:
  - get
